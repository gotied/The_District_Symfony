{% extends 'base.html.twig' %}

{% block title %}Panier{% endblock %}

{% block body %}
<h1>Panier</h1>
    {% if contenuPanier is empty %}
        <p>Votre panier est vide.</p>
    {% else %}
        <ul>
            {% for platId, quantite in contenuPanier %}
                <li>Plat ID: {{ platId }}, Quantité: {{ quantite }}</li>
            {% endfor %}
        </ul>
    {% endif %}
{% endblock %}
